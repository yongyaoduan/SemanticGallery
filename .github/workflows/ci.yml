name: CI

on:
  push:
  pull_request:

jobs:
  swift-tests:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Run Swift tests
        run: swift test

      - name: Validate shell scripts
        run: bash -n scripts/*.sh
